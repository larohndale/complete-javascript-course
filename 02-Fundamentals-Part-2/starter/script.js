'use strict';

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriversLicense = true;
if (hasDriversLicense) console.log('I can drive');

/* These are reserved kewords so 'use strict' 
    or strict mode will not allow these reservered keywords */
// const interface = 'Audio';
// const private = 2;
